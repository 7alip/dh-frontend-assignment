import React from "react";
import { mount } from "enzyme";

import SearchForm from "../components/SearchForm";

describe("<SearchForm />", () => {
  it("should set dropdown direction down as default", () => {
    // expect(mount(<SearchForm />).props().dir).to.equal("down");
  });
});
